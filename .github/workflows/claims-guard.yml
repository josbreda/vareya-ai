name: Prohibited Claims Guard

on:
  pull_request:
    branches: [main]

jobs:
  claims-guard:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run prohibited-claims scan
        run: bash scripts/prohibited-claims-scan.sh
