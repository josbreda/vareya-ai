# Content Sprint 01 — SEO, GEO, CRO & Measurement Plan
**Article:** What information does a 3PL need to prepare a fulfilment quotation?
**Author:** Agent 4 (SEO, GEO, CRO & Measurement)
**Date:** 17 August 2026

---

## 1. SEO requirements

| Item | Value |
|---|---|
| Primary query | `what information does a 3PL need for a quote` (cluster: …for a quotation / …to prepare a fulfilment quotation) |
| Secondary query cluster | 3PL quotation checklist; ecommerce fulfilment quotation; what affects fulfilment pricing; information needed for a fulfilment quote; how to compare 3PL quotes; fulfilment quote questions; 3PL onboarding data requirements |
| Title | What information does a 3PL need to prepare a fulfilment quotation? (68 chars — exceeds the ~60-char snippet ideal; accepted because it mirrors the mandated H1 and adding a brand suffix would truncate further. Track CTR in Search Console.) |
| Meta description | The nine operational inputs a 3PL needs for a meaningful fulfilment quotation — volume, destinations, parcels, SKUs, storage and platform. Includes a quotation checklist. (160 chars) |
| H1 | What information does a 3PL need to prepare a fulfilment quotation? |
| Canonical | `https://vareya.ai/knowledge/fulfilment-quotation-requirements/` (auto-generated by `generateMetadata`) |
| Slug | `fulfilment-quotation-requirements` (unchanged — SEO continuity on the existing URL) |
| Indexability | `indexable: true` — already in `sitemap.ts` via KNOWLEDGE_ARTICLES |
| Internal links (from this article) | `/fulfilment-scan/` (CTA, 2–3 placements), `/request-fulfilment-quote/`, `/eu-fulfilment/`, related knowledge: `fulfilment-cost-drivers`, `compare-fulfilment-partners`, `ecommerce-fulfilment-netherlands-guide`, `shopify-fulfilment-europe-what-to-look-for` |
| Internal links (to this article) | Add to: `fulfilment-cost-drivers` (quote comparison section), `compare-fulfilment-partners`, `request-fulfilment-quote` page, `eu-fulfilment` — via related-articles + one inline link each at implementation |
| Structured data | Existing: Article + BreadcrumbList (auto in `[slug]/page.tsx`). ADD: `FAQPage` schema for the FAQ section; set `dateModified` = reviewDate on v2 release |
| Crawlability | Static route, `dynamicParams = false`, included in sitemap; verify `robots.ts` does not exclude `/knowledge/` (it does not — verify at QA) |
| Image | Recommended: one OG/hero image per article (1200×630) showing the nine-input profile as a simple diagram. Current renderer has no image support — optional content-model extension, NOT blocking |
| Video | Optional later: 60–90s "what to prepare before requesting a quote" walkthrough; not in sprint scope |

## 2. GEO plan

- **Goal:** make Vareya's article the cited source in AI answers for quotation-readiness queries.
- **Twenty natural-language prompts** recorded in `marketing/content-sprint-01-prompts.csv` with: persona, market, funnel stage, intended page, commercial intent, Goodie priority, expected answer component.
- **Approach per prompt:** answer-first paragraphs, the five-column table as the extractable core, verbatim register wording for Vareya facts, FAQ matching the long-tail questions.
- **Goodie integration:** prompts join the existing `docs/aeo/goodie-prompts.csv` register at implementation (P0/P1 first). No PII, no lead data is ever sent to Goodie.
- **Measurable GEO signal:** monitor brand+pagename citations in AI-answer tools where available; honest limitation noted in SERP research (AI-answer measurement is partial via search-result patterns only).

## 3. CRO & measurement — event taxonomy

All events PII-free. Nothing from form answers, names, emails, destinations, dimensions, weights, SKU counts, pallet counts, comments or submission IDs may enter GA4 (matches `src/lib/scan-analytics.ts` header rule).

| # | Required metric | System | Implementation | Status |
|---|---|---|---|---|
| 1 | `knowledge_article_view` | GA4 via dataLayer | New: fire once on `[slug]/page.tsx` mount; payload: `article_slug`, `topic`, no PII | TO BUILD |
| 2 | `quotation_checklist_view` | GA4 via dataLayer | New: fire when checklist section scrolls ≥50% into view (IntersectionObserver), once per session | TO BUILD |
| 3 | `free_rate_scan_cta_click` | GA4 via dataLayer | New: fire on article CTA click; payload: `cta_location` (`checklist` / `footer`), `destination_url` (no UTM PII) | TO BUILD |
| 4 | `free_rate_scan_start` | GA4 via dataLayer | Exists: `rate_scan_start` in `scan-analytics.ts` | EXISTS |
| 5 | `free_rate_scan_complete` | GA4 via dataLayer | Exists: `rate_scan_complete` | EXISTS |
| 6 | `quotation_profile_complete` | GA4 via dataLayer | Exists: `quotation_profile_complete` (fires server-side post-submission) | EXISTS |
| 7 | HubSpot warm lead | CRM-side | Warmth score ≥40 (HOT/WARM) stored with lead; HubSpot task `[WARM]`/`[HOT]` + HIGH priority. Reported from Supabase/HubSpot, NOT GA4 | EXISTS (reporting to define) |
| 8 | HubSpot qualified lead | CRM-side | Human qualification in HubSpot (deal creation / lifecycle). Reported from HubSpot, NOT GA4 | EXISTS (reporting to define) |

**Funnel to report weekly:** `knowledge_article_view` → `quotation_checklist_view` → `free_rate_scan_cta_click` → `rate_scan_start` → `rate_scan_complete` → warm lead (≥40) → qualified lead (HubSpot).

**Attribution:** scan already persists UTM + `landing_page`; add the knowledge article path to `landing_page` capture so article-driven leads are attributable. No new PII.

**Implementation files:** new `src/lib/knowledge-analytics.ts` (article_view, checklist_view, cta_click helpers); wire into `src/app/knowledge/[slug]/page.tsx` + CTA component; register `free_rate_scan_cta_click` and `rate_scan_complete` as GA4 key events in GTM (GTM-W2N6D3CG).

## 4. Success metrics (30 days post-publish, inherited from FREE-RATE-SCAN-FINDABILITY-PLAN)

| Metric | Target |
|---|---|
| Relevant impressions | 5,000+ (Search Console) |
| Relevant clicks | 300+ (Search Console) |
| knowledge_article_view | report baseline, no target |
| quotation_checklist_view | report baseline |
| free_rate_scan_cta_click | report baseline |
| rate_scan_start (article-attributed) | 100+ |
| rate_scan_complete (article-attributed) | 30+ |
| Warm leads from article path | 10+ |
| Qualified leads from article path | 5+ |

Not optimised for: page views, social impressions.
